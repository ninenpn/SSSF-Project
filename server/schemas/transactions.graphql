type Transaction {
    id: ID!
    amount: Float!
    sender: User!
    receiver: User!
    reference: String!
    status: String!
}

type Query {
    transactions: [Transaction!]!
    transactionById(id: ID!): Transaction
}

type Mutation {
    deleteTransaction(id: ID!): Transaction!
}